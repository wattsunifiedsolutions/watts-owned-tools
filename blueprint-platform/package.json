{
  "name": "watts-blueprint-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name watts-blueprint --branch main"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
